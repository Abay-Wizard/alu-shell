#!/usr/bin/env bash
# List files in current directory, showing only the part after the first dash

for file in *; do
    if [[ "$file" == *-* ]]; then
        echo "${file#*-}"
    else
        echo "$file"
    fi
done
